VIRTUAL_HOST=localhost
USE_TLS=false

# KEY_FILE_PATH={path to key file, used when "USE_TLS" is set to true}
# CERT_FILE_PATH={path to certificate file, used when "USE_TLS" is set to true}
# STUN_SERVERS={list of IPv4 addresses of the STUN servers}
# TURN_SERVERS={list of IPv4 addresses of the TURN servers}
# EXTERNAL_IP={the IP address, on which TURN servers will listen}
